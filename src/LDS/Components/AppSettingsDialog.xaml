<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="LDS.Components.AppSettingsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LDS.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance Type=local:AppSettingsDialog}"
    mc:Ignorable="d">

    <Page.Resources>
        <Style TargetType="Border" x:Key="ContainerStyle">
            <Setter Property="BorderBrush" Value="#1cffffff" />
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="Background" Value="#aa0c0c0e" />
        </Style>
    </Page.Resources>

    <StackPanel>
        <Grid ColumnSpacing="10" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Enable leash feature"  VerticalAlignment="Center" MinWidth="200" />
            <ToggleSwitch Grid.Column="1" IsOn="{x:Bind Settings.GlobalEnableLeash, Mode=TwoWay}" VerticalAlignment="Center" OnContent="" OffContent="" MinWidth="0" />
            <FontIcon Grid.Column="2" Glyph="&#xE946;" ToolTipService.ToolTip="This globally disables the leash movement feature" VerticalAlignment="Center" />
        </Grid>

        <Rectangle Height="1" Fill="#33ffffff" Margin="0,10"/>

        <Grid ColumnSpacing="10" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Enable counter feature"  VerticalAlignment="Center" MinWidth="200" />
            <ToggleSwitch Grid.Column="1" IsOn="{x:Bind Settings.GlobalEnableLeash, Mode=TwoWay}" VerticalAlignment="Center" OnContent="" OffContent="" MinWidth="0" />
            <FontIcon Grid.Column="2" Glyph="&#xE946;" ToolTipService.ToolTip="This globally disables the counter feature" VerticalAlignment="Center" />
        </Grid>
        
        <Rectangle Height="1" Fill="#33ffffff" Margin="0,10"/>

        <Grid ColumnSpacing="10" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Allow Leash toggle" VerticalAlignment="Center" MinWidth="200" />
            <ToggleSwitch Grid.Column="1" IsOn="{x:Bind Settings.EnableToggleOnNullInput, Mode=TwoWay}" VerticalAlignment="Center" OnContent="" OffContent="" MinWidth="0" />
            <FontIcon Grid.Column="2" Glyph="&#xE946;" ToolTipService.ToolTip="This option allows the app to toggle off and on the leash when the distance to the colliders is 0. This can happen for quite a number of reasons and toggling it ususally solves it." VerticalAlignment="Center" />
        </Grid>

        <Rectangle Height="1" Fill="#33ffffff" Margin="0,10"/>

        <Grid ColumnSpacing="10" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="Calculator type" VerticalAlignment="Center" MinWidth="200" />
            <ComboBox Grid.Column="1" ItemsSource="{x:Bind CalculatorOptions, Mode=OneTime}" SelectedItem="{x:Bind Settings.CalculatorType, Mode=TwoWay}" HorizontalAlignment="Center" MinWidth="150" />
            <FontIcon Grid.Column="2" Glyph="&#xE946;" ToolTipService.ToolTip="{x:Bind Settings.CalculatorDescription, Mode=OneWay}" HorizontalAlignment="Right" />
        </Grid>

        <Rectangle Height="1" Fill="#33ffffff" Margin="0,10"/>

        <Button Content="Open log directory" Command="{x:Bind OpenLogDirectoryCommand}" HorizontalAlignment="Center" Margin="0 5 0 0" />

    </StackPanel>
</Page>
