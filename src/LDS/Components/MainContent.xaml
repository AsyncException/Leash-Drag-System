<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="LDS.Components.MainContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LDS"
    xmlns:components="using:LDS.Components"
    xmlns:converters="using:LDS.Converters"
    xmlns:utilities="using:LDS.Utilities"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converters:VisibilityConverter x:Name="visibility" />
        <converters:InvertedVisibilityConverter x:Name="invVisibility" />
        <utilities:ImageLoader x:Key="UnityImage" Name="LDS.Assets.unity.png" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Leash Drag System" VerticalAlignment="Center" Margin="10 5 5 5" Height="30"/>
        </StackPanel>

        <NavigationView x:Name="Navigation"
                Grid.Row="1"
                SelectionChanged="Navigation_SelectionChanged"
                PaneDisplayMode="LeftCompact"
                IsPaneToggleButtonVisible="False"
                IsSettingsVisible="False"
                IsBackButtonVisible="Collapsed">

            <NavigationView.Resources>
                <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent"/>
            </NavigationView.Resources>

            <NavigationView.MenuItems>

                <NavigationViewItem Tag="HomePage" Content="Home">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE890;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Tag="ValuesPage" Content="Values">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE90F;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>

                <NavigationViewItem Tag="Connection" Content="Connection" IsEnabled="False">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="{x:Bind ConnectionStatus.ConnectionIcon, Mode=OneWay}" Foreground="{x:Bind ConnectionStatus.ConnectionColor, Mode=OneWay}" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Tag="Unity" Content="Unity">
                    <NavigationViewItem.Icon>
                        <BitmapIcon UriSource="/Assets/unity.png" Width="20" Height="20" Foreground="{x:Bind ConnectionStatus.UnityColor, Mode=OneWay}" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Tag="Console" Content="Console">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEC7A;" FontSize="20" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Tag="SettingsPage" Content="Settings">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE713;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

            </NavigationView.FooterMenuItems>

            <Frame x:Name="ContentFrame" />
        </NavigationView>
    </Grid>

    

    <!--
    <Grid ColumnSpacing="15" Padding="15 0 15 15">

        <Grid.Resources>
            <Style TargetType="Border" x:Key="ContainerStyle">
                <Setter Property="BorderBrush" Value="#1cffffff" />
                <Setter Property="BorderThickness" Value="2" />
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="Padding" Value="10" />
                <Setter Property="Background" Value="#aa0c0c0e" />
            </Style>
        </Grid.Resources>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="35" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Stretch" ColumnSpacing="25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="Leash Drag System" Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Stretch" Padding="10"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="10">
                <TextBlock Text="Connected:" VerticalAlignment="Center" />
                <FontIcon Glyph="&#xE701;" Foreground="Green" Visibility="{x:Bind ConnectionStatus.IsConnected, Mode=OneWay, Converter={StaticResource visibility}}" />
                <FontIcon Glyph="&#xEB5E;" Foreground="Red" Visibility="{x:Bind ConnectionStatus.IsConnected, Mode=OneWay, Converter={StaticResource invVisibility}}" />
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal"  Spacing="10">
                <TextBlock Text="Send port:" VerticalAlignment="Center" />
                <TextBlock Text="{x:Bind ConnectionStatus.SendPortString, Mode=OneWay}" VerticalAlignment="Center" />
            </StackPanel>

            <StackPanel Grid.Column="3" Orientation="Horizontal" Spacing="10">
                <TextBlock Text="Send port:" VerticalAlignment="Center" />
                <TextBlock Text="{x:Bind ConnectionStatus.ReceivePortString, Mode=OneWay}" VerticalAlignment="Center" />
            </StackPanel>
            
            <StackPanel Grid.Column="4" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="5">
                <FontIcon Glyph="&#xE930;" x:Name="UnitySuccess" Visibility="Collapsed" />
                <FontIcon Glyph="&#xEA39;" x:Name="UnityError" Visibility="Collapsed" />
                <ProgressRing x:Name="UnityProgress" IsActive="True" Visibility="Collapsed" />
                <Button Command="{x:Bind StartUnityCommand}" >
                    <Image Source="{StaticResource UnityImage}" Width="20" Height="20" />
                </Button>

                <Button Command="{x:Bind ToggleConsoleCommand}" >
                    <FontIcon Glyph="&#xEC7A;" FontSize="20" />
                </Button>

                <Button Command="{x:Bind InvokeSettingsCommand}" >
                    <FontIcon Glyph="&#xE713;" FontSize="20" />
                </Button>
            </StackPanel>
        </Grid>

    </Grid>
    -->
</UserControl>
