<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="LDS.Components.MainContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LDS"
    xmlns:components="using:LDS.Components"
    xmlns:converters="using:LDS.Converters"
    xmlns:utilities="using:LDS.Utilities"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converters:VisibilityConverter x:Name="visibility" />
        <converters:InvertedVisibilityConverter x:Name="invVisibility" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Leash Drag System" VerticalAlignment="Center" Margin="10 5 5 5" Height="30"/>
        </StackPanel>

        <NavigationView x:Name="Navigation"
                Grid.Row="1"
                SelectionChanged="Navigation_SelectionChanged"
                PaneDisplayMode="LeftCompact"
                IsPaneToggleButtonVisible="False"
                IsSettingsVisible="False"
                IsBackButtonVisible="Collapsed">

            <NavigationView.Resources>
                <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent"/>
            </NavigationView.Resources>

            <NavigationView.MenuItems>
                <NavigationViewItem Tag="HomePage" Content="Home" Icon="View" />
                <NavigationViewItem Tag="ValuesPage" Content="Values" Icon="Repair" />
            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>

                <NavigationViewItem Tag="Connection" Content="Connection" IsEnabled="False">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="{x:Bind ConnectionStatus.ConnectionIcon, Mode=OneWay}" Foreground="{x:Bind ConnectionStatus.ConnectionColor, Mode=OneWay}" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Tag="Unity" Content="Unity">
                    <NavigationViewItem.Icon>
                        <BitmapIcon UriSource="/Assets/unity.png" Width="20" Height="20" Foreground="{x:Bind ConnectionStatus.UnityColor, Mode=OneWay}" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Tag="Console" Content="Console">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEC7A;" FontSize="20" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem Tag="SettingsPage" Content="Settings">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE713;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

            </NavigationView.FooterMenuItems>

            <Frame x:Name="ContentFrame" />
        </NavigationView>
    </Grid>
</UserControl>
