<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="LDS.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LDS"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="LDS">

    <Grid ColumnSpacing="15" Padding="15 0 15 15">
        
        <Grid.Resources>
            <Style TargetType="Border" x:Key="ContainerStyle">
                <Setter Property="BorderBrush" Value="#1cffffff" />
                <Setter Property="BorderThickness" Value="2" />
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="Padding" Value="10" />
                <Setter Property="Background" Value="#aa0c0c0e" />
            </Style>
        </Grid.Resources>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="35" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Stretch">
            <TextBlock Text="Leash Drag System" VerticalAlignment="Center" HorizontalAlignment="Stretch" Padding="10"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 1 135 1" Spacing="5">
                <Button Command="{x:Bind ToggleConsoleCommand}" >
                    <FontIcon Glyph="&#xEC7A;" FontSize="20" />
                </Button>

                <Button Command="{x:Bind InvokeSettingsCommand}" >
                    <FontIcon Glyph="&#xE713;" FontSize="20" />
                </Button>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Padding="12" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <local:ValueStatistics Grid.Row="0" />
                <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">

                    <Grid VerticalAlignment="Stretch">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="auto" />
                        </Grid.RowDefinitions>
                        
                        <local:Settings Grid.Row="1" />

                    </Grid>
                </ScrollViewer>
            </Grid>

        </Border>

        <Border Grid.Row="1" Grid.Column="1" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <local:AvatarVisualizerCanvas />
        </Border>

        <Grid Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" Grid.RowSpan="2" x:Name="SettingsPopup" Visibility="Collapsed">
            <Border Margin="0 35 0 0" Background="#aa0c0c0e" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" CornerRadius="10">
                <local:AppSettings />
            </Border>
        </Grid>
        
    </Grid>
</Window>
